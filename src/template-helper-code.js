const managerFunction = (name, id, email, officeNumber) =>
<div class="card">
${name}
${id}
${email}
${officeNumber}
</div>
;

const engineerFunction = (name, id, email, github) =>
<div class="card">
${name}
${id}
${email}
${github}
</div>
;

const internFunction = (name, id, email, officeNumber) =>
<div class="card">
${name}
${id}
${email}
${school}
</div>
;

const mainBody = () => {
<div class="card">
${name}
${id}
${email}
${officeNumber}
</div>
};